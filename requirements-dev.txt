# เครื่องมือสำหรับพัฒนา/CI เท่านั้น — ผู้ใช้ทั่วไปไม่ต้องติดตั้ง (install.bat ใช้ requirements.txt)
# เวอร์ชันล็อกไว้ให้ตรงกับที่ใช้ตั้งค่า CI ครั้งแรก เพื่อให้ผล lint/เทสต์ตรงกันทุกเครื่อง
pytest==9.1.1
ruff==0.16.8
pyinstaller==6.10.0  # สร้าง binary เดียว (soonai.exe) — รันได้ไม่ต้องมี Python
